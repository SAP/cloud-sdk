Similar to the function import mentioned above, the [action import request builder](https://sap.github.io/cloud-sdk/api/1.32.1/classes/sap_cloud_sdk_core.actionimportrequestbuilder) has the same structure, so you can build action requests the same way as the function import.
