By default, the SAP Cloud SDK uses `axios` as HTTP client for executing requests.
In addition to basic options like `url` and `headers`, being generated by the SAP Cloud SDK under the hood, you are also allowed to provide custom request configuration, which is then used as custom `axios` options by default.
The example below demonstrates how to indicate the response data type, which is typically used when downloading a file from an endpoint.
