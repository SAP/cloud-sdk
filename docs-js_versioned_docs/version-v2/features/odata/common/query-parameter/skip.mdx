`skip` allows you to skip a number of results in the requested set.
It can be useful for paging:

```ts
const { businessPartnerApi } = businessPartnerService();
businessPartnerApi.requestBuilder().getAll().skip(10);
```
